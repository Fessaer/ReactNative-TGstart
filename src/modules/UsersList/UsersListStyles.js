import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  containerList: {
    flexGrow: 1,
  },
  container: {
    flex: 1,
    position: 'absolute',
    top: -30.28,
    left: 0,
    right: 0,
    bottom: 0,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

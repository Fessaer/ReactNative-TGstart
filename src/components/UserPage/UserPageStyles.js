import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  container: {
    color: 'white',
    padding: 12,
    width: '100%',
    height: '100%',
  },
  image: {
    width: '100%',
    height: '100%',
  },
});
